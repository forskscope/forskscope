# CLI Usage

```sh
# Open two files immediately
forskscope <left> <right>

# Open Explorer workspace
forskscope
```

The left path is the old/source side; the right is the new/target/result side.

Planned: directory pair startup, `--no-gui` diff report output (RFC-027).
