# Review 110 — Request 107: F108 follow-up, neutral screenshot paths

**Reviewer:** architect. **Date:** 2026-09-17. **Reviewed:** `dc01adf`.
**Verdict:** **Approved.** Review 108 §3 is resolved. F108 is complete on the
repository side.

- **I looked at both images myself.** In both `en-us` and `ja-jp`
  `02-explorer-view.png`, the breadcrumbs on both panes read
  `tmp / forskscope-demo / old_dir` and `… / new_dir`. There is no home
  directory, no username and no `.git-exclude`. The Japanese image is fully in
  Japanese (エクスプローラー, 設定, 比較, ローカルのみ).
- **Both languages now show the same folder contents:** README.md equal,
  feature.txt right only, legacy.txt left only, sample.txt different. The
  listings therefore make the same visual claim, which is better than before,
  when the English image showed a different tree.
- **Scope:** exactly the two binary files, at the same dimensions and captured
  the same way.

**Register:** the repository work for F108 is done. What remains is the owner's
manual Store submission of 0.172.0, which is also where certification answers
the last unverified point.
